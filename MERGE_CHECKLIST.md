# MERGE_CHECKLIST — สาขา `preview/job-template-picking`

เอกสารนี้คือ **ขั้นตอนบังคับตอนรวมสาขานี้เข้า main และ deploy ขึ้น production**
เขียนไว้เพราะของหลายอย่างในสาขานี้ "เสร็จแล้วในโค้ด แต่ยังไม่เสร็จในโลกจริง"
จนกว่าจะมีคนไปทำอะไรบางอย่างนอก repo — ถ้าลืม ระบบจะพังแบบเงียบ ๆ หรือช่องโหว่จะเปิดค้างไว้

ผู้ที่ทำตามเอกสารนี้ไม่จำเป็นต้องเคยอยู่ในงานที่สร้างสาขานี้ ทุกข้อมีคำสั่งตรวจของตัวเอง

---

## 0. ข้อเท็จจริงที่ต้องเข้าใจก่อน ไม่งั้นจะทำผิดลำดับ

1. **โปรเจกต์ Supabase มีตัวเดียว ไม่มี staging** (เป็น Ruling ที่ตั้งใจ — ไม่ซื้อ Supabase branch)
   แปลว่า **migration ทุกตัวของสาขานี้ถูก apply ลงฐานข้อมูล production ไปแล้ว**
   ตั้งแต่ตอนพัฒนา ไม่ใช่ตอน merge
2. เพราะข้อ 1 ตอนนี้ **ฐานข้อมูลล้ำหน้าโค้ดบน main อยู่** — production รันโค้ดเก่า
   บนฐานข้อมูลที่มีของใหม่ครบแล้ว ระบบไม่พังเพราะโค้ดเก่าไม่รู้จักของใหม่ จึงไม่ไปแตะ
3. เพราะข้อ 2 **การ merge รอบนี้แทบไม่ใช่งานฐานข้อมูล แต่เป็นงาน "สลับหน้าบ้าน"**
   และมีสองสามอย่างที่ต้องทำ **หลัง** หน้าบ้านใหม่ขึ้นแล้วเท่านั้น (ข้อ 4 ด้านล่าง)
4. สาขานี้เพิ่ม migration 58 ไฟล์ · ตารางใหม่ 12 ตาราง · ไม่มีการแก้ไฟล์ migration ที่ apply ไปแล้ว

---

## 1. [ก่อน deploy] ตั้งค่า environment variable บน Vercel

cron สามตัวใน `vercel.json` จะยิงตามเวลาทันทีที่ deploy สำเร็จ ถ้ายังไม่ได้ตั้งค่า
มันจะยิงแล้วโดนปฏิเสธเงียบ ๆ ทุกวัน โดยไม่มีอะไรฟ้องว่าไม่เคยทำงานเลย

ตั้งบน Vercel project ของ floor (Environment = Production) **ก่อน** promote deployment:

| ชื่อ env var | ใช้ที่ | ถ้าไม่ตั้งจะเกิดอะไร |
|---|---|---|
| `STOCK_SHORTAGE_CRON_SECRET` | `app/api/stock/shortage-check/route.ts` (cron `0 0 * * *`) | ถ้าไม่มีทั้งตัวนี้และ `CRON_SECRET` → endpoint ตอบ 401 ทุกครั้ง = ไม่มีการเตือนของขาดล่วงหน้าเลย |
| `STOCK_SHORTAGE_LOOKAHEAD_DAYS` | `lib/stock-shortage.ts` | ไม่บังคับ · ไม่ตั้ง = ใช้ค่าปริยาย 7 วัน (เพดาน 60) ตั้งเมื่ออยากได้ช่วงมองล่วงหน้าอื่น |
| `PROVIDER_EVAL_CRON_SECRET` | `app/api/providers/eval-recompute/route.ts` (cron `0 19 * * *`) | ถ้าไม่มีทั้งตัวนี้และ `CRON_SECRET` → 401 ทุกครั้ง = คะแนนทีมช่าง/ผู้รับเหมาไม่เคยถูกคำนวณใหม่ |

หมายเหตุสำคัญ: ทั้งสอง route **ถอยไปใช้ `CRON_SECRET` ได้** ถ้าตัวเฉพาะไม่มี
ถ้าในโปรเจกต์ตั้ง `CRON_SECRET` ไว้อยู่แล้ว ระบบจะทำงาน — แต่ยังควรตั้งตัวเฉพาะ
เพราะการหมุนคีย์ของงานหนึ่งจะได้ไม่ไปดับ cron ตัวอื่นพร้อมกัน

**ห้ามใส่ค่าของ secret ลงใน repo หรือในเอกสารนี้** — ที่นี่บันทึกได้แค่ "ชื่อ"

ตรวจหลังตั้ง (ต้องได้ 200 ไม่ใช่ 401):

```
curl -s -o /dev/null -w '%{http_code}\n' \
  -H "Authorization: Bearer <ค่าที่ตั้งไว้>" \
  https://<โดเมน production>/api/stock/shortage-check
```

---

## 2. [ก่อน deploy] ตรวจว่าไฟล์ migration ตรงกับที่ apply ลงฐานข้อมูลจริง

ระหว่างพัฒนาสาขานี้เครื่องหลุดหลายรอบ ทำให้เคยมี migration ที่ apply ลงฐานข้อมูลแล้ว
แต่ไฟล์หายไปจาก repo รวม 11 ตัว (กู้แล้วในคอมมิต `ac0989e` 9 ตัว และ `b1a8890` อีก 2 ตัว)
ก่อน merge ให้กวาดซ้ำอีกครั้ง เพราะสาขานี้ยังมีการ apply เกิดขึ้นได้อีก

รันบนฐานข้อมูล production แล้วเทียบกับรายชื่อไฟล์ใน `supabase/migrations/`
(ชื่อไฟล์ในสาขานี้ **ไม่ได้ใช้เลขเวลาเดียวกับ `version` ในฐานข้อมูล** — ให้เทียบด้วย "ชื่อ" ไม่ใช่เลข):

```sql
select version, name
from supabase_migrations.schema_migrations
where version >= '20260831200000'
order by version;
```

```bash
ls supabase/migrations/ | sed -E 's/^[0-9]+_//; s/\.sql$//' | sort
```

ชื่อที่อยู่ในฐานข้อมูลแต่ไม่มีไฟล์ = drift ต้องกู้ไฟล์ก่อน merge
(วิธีกู้: อ่าน `supabase_migrations.schema_migrations.statements` ของแถวนั้น แล้วเขียนเป็นไฟล์
ให้รันซ้ำได้ ตามแบบไฟล์ใน `ac0989e`) — **ห้ามแก้ไฟล์ migration ที่ apply ไปแล้ว**
ฐานข้อมูลที่ไม่มีใคร replay ได้ คือฐานข้อมูลที่กู้คืนไม่ได้

หมายเหตุ: ฐานข้อมูลนี้ใช้ร่วมกับ repo อื่น (bbps-crm, HR, IIM, Error Monitor)
ชื่อที่ไม่ใช่ของ floor ไม่ใช่ drift ของ repo นี้ ชื่อที่ยืนยันแล้วว่าเป็นของ repo อื่น ณ วันที่เขียน:
`add_b2b_b2c_commercial_context` · `shift_type_late_grace` · `errmon_init` ·
`errmon_lock_down_increment_rpc` · `add_hospital_media_attributions` ·
`index_hospital_media_batch` · `add_cross_sell_campaign_audience` · `add_iim_message_media_urls`

ระวังกับดักการเทียบชื่อ: `ncr_reports_read_policy_and_catalog_definer` ในฐานข้อมูล
คือไฟล์ `20260902140020_ncr_reports_read_policy.sql` (ชื่อไม่ตรงเป๊ะแต่เนื้อตรง —
ตรวจแล้วว่ามีทั้ง policy และ `floor_receipt_reason_catalog()` ครบ) ไม่ใช่ drift

### 🔴 drift ของ floor ที่ยังค้างอยู่ ณ วันที่เขียนเอกสารนี้

สองตัวนี้เป็นของ floor แน่นอน (สร้าง `public.floor_csat_automation_jobs` และเพิ่มคอลัมน์
`source_evaluation_id` ให้ `public.floor_after_sales_cases`) apply ลงฐานข้อมูลไปแล้ว
แต่ **ไม่มีไฟล์ใดใน `supabase/migrations/` ทั้ง repo**:

| version | name |
|---|---|
| `20260901014326` | `csat_after_sales_automation` |
| `20260901014516` | `csat_automation_fk_indexes` |

ที่มา: มาจาก PR #125 (NCR/After-sales/CSAT) ซึ่งอยู่บน **main** อยู่ก่อนสาขานี้แล้ว
(คอมมิต `3c164c0` เป็นบรรพบุรุษของ `b42ce0b`) จึง **ไม่ใช่ drift ที่สาขานี้ทำให้เกิด**
และตั้งใจไม่กู้ในสาขานี้ เพราะเป็นของทีมอื่นและการเอามาปนจะทำให้ diff ของสาขานี้อ่านยากขึ้น

ต้องกู้อยู่ดี — หน้า `/csat-automation` ในโค้ดพึ่งตารางนี้ ใครที่ตั้งสภาพแวดล้อมใหม่จาก
`supabase/migrations/` จะได้ระบบที่หน้านั้นพังทันที ให้ทำเป็นงานแยกบน main
(อ่าน `supabase_migrations.schema_migrations.statements` ของสองแถวนั้นแล้วเขียนเป็นไฟล์
ทั้งคู่เขียน `if not exists` มาแต่เดิมอยู่แล้ว จึงรันซ้ำได้โดยไม่ต้องแก้อะไร)

---

## 3. [deploy] ขึ้นหน้าบ้านใหม่

1. `npx tsc --noEmit` ต้อง exit 0
2. `npx vitest run` ต้องผ่านทั้งหมด
3. merge เข้า main แล้ว deploy production

**🔴 อุปสรรคที่รู้อยู่แล้ว:** เครื่องที่ใช้พัฒนา **push ไม่ได้**
(ไม่มี credential ฝั่งเครื่อง และ git proxy ตอบ 403 `DevTeamMPD/floor is not in this
session's authorized repository set`) งานถูกเก็บเป็น bundle ไว้ที่
`Desktop\Floor\preview-job-template-picking.bundle` (ต้องมีคอมมิต `b42ce0b` เป็นฐาน)
ก่อนทำข้อ 4 ต้องยืนยันก่อนว่าเนื้อสาขาขึ้นไปถึง remote จริง ไม่ใช่แค่อยู่บนเครื่อง

---

## 4. [🔴 หลัง deploy เท่านั้น] ถอนสิทธิ์ฟังก์ชันปิดงานรุ่นเก่าที่เดินอ้อมด่านตรวจรับได้

### ทำไมต้องมีข้อนี้

สาขานี้เพิ่ม "ด่านตรวจรับ" (`job_acceptance_gate`) เข้าไปในการปิดงาน และสร้างฟังก์ชันรุ่นใหม่
สองตัวที่ผ่านด่านนั้น หน้าบ้านใหม่เรียกเฉพาะรุ่นใหม่แล้ว

| เดิม (ยังมีอยู่ ยังเรียกได้) | ใหม่ (สาขานี้ใช้) |
|---|---|
| `close_floor_work_order_cs_v3(uuid)` | `close_floor_work_order_cs_v4(uuid)` |
| `close_floor_work_order_special(uuid, text)` | `close_floor_work_order_special_v2(uuid, text, boolean)` |

สถานะจริงของสองตัวเก่า ณ วันที่เขียน (ยืนยันจาก `pg_proc`):
`security definer` · `search_path=public` (ไม่ใช่ `''`) · **`authenticated` ยังมีสิทธิ์ EXECUTE**

แปลว่า **ใครก็ตามที่ล็อกอินได้ ยิง RPC ตรง ๆ ก็ปิดงานได้โดยไม่ผ่านด่านตรวจรับ**
ด่านที่เดินอ้อมได้ไม่ใช่ด่าน

### ทำไมยังถอนสิทธิ์ตอนนี้ไม่ได้

production (main) **ยังรันโค้ดเก่าที่เรียก v3 อยู่** ถอนวันนี้ = ทั้งบริษัทปิดงานไม่ได้ทันที
การถอนจึงต้องเกิดขึ้น **ที่วินาทีที่หน้าบ้านใหม่ขึ้น production แล้วเท่านั้น** ไม่ใช่ก่อน ไม่ใช่ลืม

### ขั้นตอน

1. ยืนยันก่อนว่า deployment ใหม่ Ready และเป็น production alias จริง
2. ยืนยันว่าไม่มีโค้ดไหนเรียกตัวเก่าแล้ว (ทั้ง repo เหลือแค่ 4 ที่ และเป็นตัวใหม่ทั้งหมด):

```bash
grep -rn "close_floor_work_order_cs_v3\|close_floor_work_order_special\b" \
  --include=*.ts --include=*.tsx . | grep -v node_modules
```

3. ทดสอบปิดงานจริงหนึ่งใบผ่านหน้าจอใหม่ (ทั้งทาง CS และทาง "ปิดพิเศษ") ให้ผ่าน
4. จึงรัน — **เขียนเป็นไฟล์ migration ใหม่แล้ว commit ก่อน apply** ห้าม apply ลอย ๆ:

```sql
revoke execute on function public.close_floor_work_order_cs_v3(uuid) from authenticated;
revoke execute on function public.close_floor_work_order_special(uuid, text) from authenticated;
```

5. ตรวจผล — `authenticated` ต้องหายไปจาก `acl` ของสองตัวเก่า และยังอยู่ของสองตัวใหม่:

```sql
select p.proname, coalesce(array_to_string(p.proacl,' | '),'(default)') as acl
from pg_proc p join pg_namespace n on n.oid = p.pronamespace
where n.nspname = 'public'
  and p.proname in ('close_floor_work_order_cs_v3','close_floor_work_order_special',
                    'close_floor_work_order_cs_v4','close_floor_work_order_special_v2');
```

### ถ้าต้องถอยกลับ

`grant execute on function ... to authenticated;` คืนได้ทันที ไม่มีข้อมูลเสียหาย
จึงเป็นขั้นตอนที่ปลอดภัยที่จะลองในเวลางาน — ตราบใดที่ทำ **หลัง** หน้าบ้านใหม่ขึ้นแล้ว

### หมายเหตุ

ยังไม่ `drop function` สองตัวเก่าในรอบนี้ เพราะ drop แล้วถอยกลับไม่ได้ในทันที
เมื่อผ่านไปหนึ่งรอบ deploy ที่ไม่มีปัญหา จึงค่อยลบทิ้งเป็นงานเก็บกวาดต่างหาก
(ตอนลบ ให้ลบพร้อมกันกับการรื้อ `search_path=public` ที่ค้างอยู่)

---

## 5. [ต้องมีคำตอบจากผู้ใช้] bucket `job-photos` เป็น public — จะเอาอย่างไร

### สถานะจริง

`storage.buckets` → `job-photos` : `public = true` · ไม่จำกัดขนาดไฟล์ · ไม่จำกัดชนิดไฟล์
(สถานะนี้ **มีมาก่อนสาขานี้** สาขานี้ไม่ได้แก้ และห้ามแก้เองด้วย)

### ทำไมมันสำคัญขึ้นกว่าเดิมเพราะสาขานี้

สาขานี้เริ่มเอา **รูปหลักฐานผลตรวจรับตาม ISO 8.6** ไปเก็บใน bucket เดียวกันนี้
ที่ path `acceptance/<เลขงาน>/<รหัสเกณฑ์>/<เวลา>-<ลำดับ>-<ชื่อไฟล์>`
รูปแบบ path เดาได้ และ bucket เป็น public แปลว่า **หลักฐานการตรวจรับงานลูกค้า
เปิดอ่านได้โดยไม่ต้องล็อกอิน ถ้ารู้หรือเดาเลขงานถูก**

เดิม bucket นี้เก็บรูปสำรวจหน้างานและรูปงานเสร็จอยู่แล้ว ความเสี่ยงจึงไม่ใช่ของใหม่
แต่ "หลักฐานคุณภาพที่ผูกกับชื่อลูกค้าและเลขงาน" เป็นข้อมูลคนละชั้นกับรูปหน้างานทั่วไป

### สามทางเลือก — ต้องเลือกหนึ่ง ไม่ใช่ปล่อยผ่าน

| ทาง | ได้อะไร | เสียอะไร / ต้องแก้อะไรตาม |
|---|---|---|
| ก. ปล่อย public ต่อ | ไม่ต้องแก้โค้ดเลย | ยอมรับอย่างเป็นทางการว่าหลักฐานตรวจรับเปิดสาธารณะ ควรบันทึกเป็นการตัดสินใจไว้ ไม่ใช่ปล่อยเพราะลืม |
| ข. เปลี่ยนเป็น private + ใช้ signed URL | ปิดช่องได้จริง | ต้องแก้ทุกที่ที่เรียก `getPublicUrl` ให้เป็น `createSignedUrl` และคิดเรื่องอายุลิงก์ · กระทบหน้าจอที่มีอยู่ก่อนสาขานี้ด้วย จึงต้องทดสอบทั้งเส้น |
| ค. แยก bucket ใหม่ (private) เฉพาะหลักฐานตรวจรับ | ปิดเฉพาะของใหม่ ไม่ไปแตะของเดิม | ต้อง migrate path เดิมของ acceptance ที่อัปโหลดไปแล้ว และมีสองวิธีอ่านรูปในโค้ดเดียวกัน |

**ห้ามให้ผู้พัฒนา/เอเจนต์ตัดสินใจแทน** — การสลับ bucket เป็น private จะทำให้รูปที่หน้าจอเดิม
แสดงอยู่กลายเป็นภาพเสียทันทีทุกหน้า นี่เป็นการตัดสินใจของเจ้าของระบบ

### เกี่ยวข้องกับข้อนี้: รูปกำพร้าใน bucket

เมื่อผู้ใช้แนบรูปหลักฐานหลายไฟล์แล้วบางไฟล์ล้ม ระบบ **จงใจไม่ลบไฟล์ที่ขึ้นสำเร็จทิ้ง**
(เหตุผลเต็มอยู่ใน `lib/job-acceptance.ts` → `ACCEPTANCE_UPLOAD_KEEP_RATIONALE`)
โดยย่อ: ทำลายหลักฐานที่ผู้ใช้เพิ่งถ่ายมา = เสียหายกู้ไม่ได้ · ไฟล์ค้างใน bucket = เสียหายกู้ได้
ผลข้างเคียงคือถ้าผู้ใช้ปิดลิ้นชักทิ้งโดยไม่กดบันทึก จะเหลือไฟล์ที่ไม่มีอะไรอ้างถึง
**ถ้าเลือกทาง ก. (คง public)** ควรวางงานกวาดไฟล์ที่ไม่ถูกอ้างถึงเป็นงานประจำเพิ่มด้วย

---

## 6. เรื่องที่ merge ต้องรู้ แต่ไม่บล็อกการ deploy

ทั้งหมดนี้เป็นของค้างที่ถูก **ตัดสินใจไว้แล้วว่าจะยังไม่ทำในรอบนี้** ไม่ใช่ของที่ลืม
บันทึกไว้เพื่อไม่ให้ใครนึกว่าเป็นบั๊กแล้วรีบแก้ผิดจุด

1. **ไม่มีการจองของข้ามงาน** — สองงานวันเดียวกันที่ใช้ SKU เดียวกัน จะเห็นว่า "ของพอ" ทั้งคู่
   หน้าจอเขียนบอกแล้วว่าเช็คทีละงาน ไม่ได้หักงานอื่น · การจองจริงเป็นงานรอบถัดไป
2. **ช่างยังบันทึกผลตรวจรับเองไม่ได้** — RPC `save_job_acceptance_results` จำกัดที่
   `admin` / `head_technician` เป็นการตัดสินใจเชิงนโยบาย ไม่ใช่ข้อจำกัดทางเทคนิค
   ถ้าต้องการให้ช่างบันทึกเองต้องแก้เป็นเรื่องเป็นราว ไม่ใช่คลายสิทธิ์เฉย ๆ
3. **`job_prep_template_items` ไม่มีคอลัมน์ `is_active`** ต่างจากฝั่ง checklist ที่มี
   spec เดิมไม่ได้ขอ · ถ้าต้องการความสมมาตรให้ทำเป็นงานแยก
4. **หน้า admin 27 หน้า ยังไม่มี guard ในหน้า 20 หน้า** (กันแค่ซ่อนเมนูในแถบข้าง)
   เป็นของเดิมก่อนสาขานี้ · เข้าคิวเป็นงาน 5-6 แล้ว
5. **`close_floor_work_order_cs_v3` / `close_floor_work_order_special` ยังใช้
   `search_path=public`** จะถูกจัดการพร้อมตอน drop (ดูข้อ 4 ท้ายหัวข้อ)
6. **สาขา `preview/cf-link` ฝั่ง bbps-crm (งาน T7 ผูก CF เข้าตั๋วงาน) ยังไม่เริ่ม**
   เป็นคนละ repo ไม่บล็อกการ merge ฝั่ง floor แต่ถ้าจะสื่อสารกับผู้ใช้ว่า "งานชุดนี้เสร็จ"
   ต้องบอกด้วยว่าฝั่ง BBPS ยังไม่ได้ทำ

---

## 7. ข้อมูลจริงที่ทำให้ฟีเจอร์ใหม่ "ยังไม่เห็นผล" หลัง deploy

ถ้าไม่รู้ข้อเหล่านี้ ทีมจะคิดว่าของใหม่พัง ทั้งที่มันทำงานถูกแล้วแต่ไม่มีข้อมูลให้ทำงานด้วย
ทั้งหมดเป็นงานกรอกข้อมูลของคน ไม่ใช่งานโค้ด

1. **ทะเบียนวัสดุ (`materials`) มีอยู่ 2 แถว** และใช้คนละชุดรหัสกับฝั่งคลัง
   ทำให้แม่แบบเตรียมของจับคู่ของได้จริงประมาณ 5 จาก 13 บรรทัดเท่านั้น
2. **`suppliers` (ผู้รับเหมาช่วง) มี 0 แถว** และรายชื่อผู้รับเหมายังไม่ถูกส่งมา
   → รายการเครื่องมือค้างคืนจะตอบไม่ได้ว่า "อยู่กับบริษัทไหน"
3. **`completed_date` ว่างทั้ง 72 งาน** → คะแนนทีมช่างจะยังไม่ปล่อยดาวให้ทีมใดเลย
   (ตั้งใจ: กันกลุ่มตัวอย่างเล็ก ไม่ปล่อยดาวถ้างาน < 3 หรือหลักฐาน < 5)
4. **ใบสั่งงาน BBPS 14 ใบ มีเพียง 1 ใบที่กรอกข้อจำกัดหน้างาน** → หน้าช่างจะว่างเป็นส่วนใหญ่
   ต่อให้แสดงผลได้ครบ ก็ไม่มีอะไรให้แสดง
5. **งานที่มีพื้นที่ ตร.ม. ใช้ได้จริง 24 จาก 116 ใบ** → การคำนวณของแบบ `per_sqm` จะข้ามงานส่วนใหญ่
6. **`job_prep_templates` ยังมี 0 แถว** → ต้องมีคนสร้างแม่แบบเตรียมของก่อนถึงจะเห็นผล
   (แม่แบบเกณฑ์ตรวจรับมีแล้ว 1 ชุด 15 ข้อ จาก seed)

---

## 8. รายการตรวจสุดท้าย (ติ๊กให้ครบ)

- [ ] 1. ตั้ง `STOCK_SHORTAGE_CRON_SECRET` บน Vercel production แล้ว
- [ ] 2. ตั้ง `PROVIDER_EVAL_CRON_SECRET` บน Vercel production แล้ว
- [ ] 3. พิจารณา `STOCK_SHORTAGE_LOOKAHEAD_DAYS` แล้ว (ตั้ง หรือจงใจใช้ค่าปริยาย 7)
- [ ] 4. กวาด drift ระหว่าง `schema_migrations` กับ `supabase/migrations/` แล้ว
        (ณ วันที่เขียน เหลือของ floor ที่ยังไม่มีไฟล์ 2 ตัว คือ `csat_after_sales_automation`
        และ `csat_automation_fk_indexes` ซึ่งเป็นของ main ไม่ใช่ของสาขานี้ — ดูข้อ 2)
- [ ] 5. `npx tsc --noEmit` exit 0 และ `npx vitest run` ผ่านทั้งหมด
- [ ] 6. เนื้อสาขาขึ้นถึง remote จริงแล้ว (ไม่ใช่แค่อยู่บนเครื่องหรือใน bundle)
- [ ] 7. deploy production Ready และเป็น alias จริงแล้ว
- [ ] 8. ทดสอบปิดงานจริงผ่านหน้าจอใหม่ ทั้งทาง CS และทางปิดพิเศษ ผ่านแล้ว
- [ ] 9. **หลังข้อ 8 เท่านั้น** — revoke `close_floor_work_order_cs_v3` และ
        `close_floor_work_order_special` จาก `authenticated` (เขียนเป็นไฟล์ migration ก่อน apply)
- [ ] 10. ตรวจ `pg_proc.proacl` ยืนยันว่า `authenticated` หายจากสองตัวเก่าแล้ว
- [ ] 11. ได้คำตอบจากเจ้าของระบบเรื่อง bucket `job-photos` แล้ว (ก / ข / ค) และบันทึกไว้
- [ ] 12. แจ้งทีมเรื่องข้อ 7 (ข้อมูลจริงที่ยังว่าง) เพื่อไม่ให้เข้าใจว่าของใหม่พัง
