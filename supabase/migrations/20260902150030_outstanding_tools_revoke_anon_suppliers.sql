-- ============================================================================
-- outstanding_tools_revoke_anon_suppliers
-- ----------------------------------------------------------------------------
-- ทำไมต้องมีไฟล์นี้
--   migration ตัวนี้ถูก apply ลงฐานข้อมูลจริงไปแล้ว (schema_migrations
--   version = 20260901094102) แต่ไฟล์ในสาขานี้หายไป — เป็น drift ชุดเดียวกับ
--   9 ตัวที่กู้ไว้ในคอมมิต ac0989e (เครื่องหลุดหลัง apply แต่ก่อนเขียนไฟล์)
--   ใครที่ replay จาก supabase/migrations/ จะได้ฐานข้อมูลไม่เหมือนของจริง
--   เนื้อ SQL ด้านล่างคัดมาจาก supabase_migrations.schema_migrations.statements
--   ของแถวนั้นตรง ๆ ไม่ใช่การเขียนใหม่จากความจำ
--
-- ตัวงานเองทำอะไร (คำอธิบายเดิมจากตอน apply เก็บไว้ครบด้านล่าง)
--   ถอน grant ค้างของ role `anon` บน public.suppliers
--   RLS policy ของตารางนี้ผูกกับ role authenticated อยู่แล้ว anon จึงอ่านไม่ได้จริง
--   ตั้งแต่แรก การ revoke นี้จึงไม่เปลี่ยนพฤติกรรมของระบบแม้แต่นิดเดียว
--   แต่ตั้งแต่ P4-2 เป็นต้นไป ชื่อผู้รับเหมาภายนอกในตารางนี้คือข้อมูลที่ระบบใช้ตอบว่า
--   "เครื่องมือที่ค้างอยู่ อยู่กับบริษัทไหน" — grant ที่ค้างและไม่มีผลคือกับดัก
--   รอวันที่ใครสักคนเผลอเขียน policy ที่กว้างกว่านี้ จึงเก็บกวาดตอนที่ยังไม่มีผลกระทบ
--
-- สถานะที่ยืนยันจากฐานข้อมูลจริงหลัง apply (information_schema.role_table_grants):
--   postgres / authenticated / service_role = สิทธิ์เต็มตามเดิม
--   anon = ไม่ปรากฏในรายการสิทธิ์ของ public.suppliers เลย
--
-- ไฟล์นี้รันซ้ำได้ (revoke เป็น idempotent โดยธรรมชาติ — revoke สิ่งที่ไม่มีอยู่ไม่ error)
-- ============================================================================

-- FloorNow P4-2 (ต่อ): ถอน grant ค้างของ anon บน suppliers
-- RLS policy ของตารางนี้ผูกกับ role authenticated อยู่แล้ว anon จึงอ่านไม่ได้จริงตั้งแต่แรก
-- แต่ตั้งแต่ P4-2 ชื่อผู้รับเหมาภายนอกเป็นข้อมูลที่ระบบใช้ตอบว่า "เครื่องมืออยู่กับบริษัทไหน"
-- จึงไม่ควรเหลือ grant ที่ทำให้คนอ่านสคีมาเข้าใจผิดว่าเปิดให้คนนอกอ่านได้
revoke all on public.suppliers from anon;
