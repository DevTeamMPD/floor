# FloorNow Operational Flow V3 — Approved Design

เอกสารนี้เป็น Flow เป้าหมายที่อนุมัติแล้วสำหรับงานขายตรงและงาน BBPS โดยกำหนดเจ้าของข้อมูล หน้าทำงาน และจุดส่งมอบระหว่างฝ่ายขาย หัวหน้าช่าง คลัง ทีมติดตั้ง CS และบุคคลภายนอกให้ชัดเจน

## หลักการสำคัญ

1. ใบสั่งงานกลางหนึ่งใบ (`floor_work_orders`) เป็นตัวควบคุมสถานะปฏิบัติงาน
2. งานขายตรงแก้ข้อมูลต้นทางใน FloorNow
3. งาน BBPS แก้ข้อมูลต้นทางใน BBPS CRM เท่านั้น แล้ว Sync กลับ FloorNow
4. ห้ามอนุมัติส่งคลังจนกว่าข้อมูลลูกค้า สถานที่ สเปก SKU จำนวน และหัวหน้าทีมครบ
5. ทุกการส่งต่อบันทึกผู้กระทำ เวลา เหตุผล และหลักฐาน
6. บุคคลภายนอกเห็นเฉพาะข้อมูลที่อนุญาตผ่าน token แบบ read-only ไม่เห็นต้นทุน สต็อก PIN หรือบันทึกภายใน

## สถานะและการส่งต่อ

```text
ฝ่ายขายสร้างงาน / BBPS Sync งาน
             |
             v
        head_review
       รอหัวหน้าตรวจ
        /          \
       /            \ อนุมัติเมื่อข้อมูล+SKU+จำนวน+หัวหน้าทีมครบ
      v              v
returned_sales   warehouse_waiting
ฝ่ายขายต้องแก้      รอคลังรับงาน
      |              |
      | ส่งตรวจใหม่  | คลังคนใดคนหนึ่งกดรับ
      +----->---------v
                 warehouse_preparing
                 กำลังเตรียมสินค้า
                        |
                        | จำนวนหยิบจริงครบ + รูปหลักฐาน
                        v
                 ready_to_install
                     รอติดตั้ง
                        |
                        | หัวหน้าทีมกดรับในวันติดตั้ง
                        v
                    installing
                   กำลังติดตั้ง
                        |
                        | รูปตาม checkpoint + ลูกค้าเซ็น
                        v
                    waiting_cs
                  รอ CS ประเมิน
                        |
                        | CS บันทึกผล / follow-up
                        v
                      closed
                     ปิดงาน
```

## กรณีหัวหน้าช่างส่งกลับฝ่ายขาย

### งานขายตรง

1. หัวหน้าช่างกด `ส่งกลับฝ่ายขาย` และต้องระบุเหตุผล
2. สถานะใบสั่งงานเปลี่ยนจาก `head_review` เป็น `returned_sales`
3. งานหายจาก Inbox รอตรวจของหัวหน้า และขึ้นหน้า `/sales-queue` แท็บ `ต้องแก้ไข`
4. ฝ่ายขายเห็นเหตุผลและช่องที่ขาด เปิดแก้ข้อมูลเดิมได้
5. ฝ่ายขายกด `บันทึกและส่งตรวจใหม่`
6. ระบบล้างเหตุผลเดิม เปลี่ยนกลับเป็น `head_review` และบันทึกประวัติ revision

### งาน BBPS

1. หัวหน้าช่างกด `ส่งกลับ BBPS` และต้องระบุเหตุผล
2. FloorNow เปลี่ยนใบสั่งงานเป็น `returned_sales` และแสดงแหล่งข้อมูลว่า `BBPS`
3. รายการตีกลับต้องขึ้น Inbox ใน BBPS CRM พร้อมเหตุผลจาก FloorNow
4. ผู้รับผิดชอบแก้ข้อมูลที่ BBPS CRM เท่านั้น
5. BBPS outbox ส่งข้อมูล revision ใหม่กลับ FloorNow
6. FloorNow เก็บ payload ใหม่ ล้างเหตุผล และเปลี่ยนกลับเป็น `head_review`
7. ห้ามแก้ raw BBPS payload โดยตรงใน FloorNow เพื่อป้องกันข้อมูลถูก Sync ทับ

## หน้าและหน้าที่ของแต่ละฝ่าย

| ฝ่าย | หน้าแรก | หน้าที่หลัก | หน้าที่รอง / รายละเอียด | ส่งต่อไป |
|---|---|---|---|---|
| ฝ่ายขายงานตรง | `/sales-queue` | จองคิว เปิดงาน กรอกลูกค้า/โทร/สถานที่/วันเวลา/สเปก/พื้นที่/รูป | แท็บ `ต้องแก้ไข`; เปิด Drawer แก้และกดส่งตรวจใหม่ | หัวหน้าช่าง |
| ฝ่ายขาย BBPS | BBPS CRM | สร้างและแก้ข้อมูล BBPS ต้นทาง รวมใบสั่งงาน วัสดุ รูป และวันติดตั้ง | Inbox `FloorNow ส่งกลับแก้ไข`; Outbox Sync revision ใหม่ | หัวหน้าช่างใน FloorNow |
| หัวหน้าช่าง | `/operations` | Inbox `รอตรวจ`, ตรวจความครบ ตีกลับ หรือเปิดอนุมัติ | `/orders/[jobNo]` เลือก SKU/จำนวน/แหล่งของ มอบหมายช่างและหัวหน้าทีม | คลังสินค้า |
| คลังสินค้า | `/warehouse` | รับงานตามลำดับ แสดงชื่อผู้รับงาน | `/orders/[jobNo]` ตรวจรายการ กรอกจำนวนหยิบจริง แนบรูป และแจ้งของขาด | หัวหน้าทีมติดตั้ง |
| ช่างรายบุคคล | `/work/[personalToken]` + PIN | ดูตารางของตน เปิดใบงาน และกดรับทราบ | ดู SKU/จำนวนจริง/สถานที่/รูป/ข้อจำกัด; อัปเดต checkpoint พร้อมรูป | หัวหน้าทีม / ลูกค้า |
| หัวหน้าทีมติดตั้ง | `/work/[personalToken]` + PIN | กดรับงานติดตั้งในวันนัดและเริ่มเดินทาง | ถึงหน้างาน → ติดตั้ง → เสร็จ → ให้ลูกค้าเซ็น | CS |
| CS | `/cs-tracking` | Inbox `รอประเมิน` โทรและบันทึกคะแนน/ปัญหา | สร้าง follow-up หากมีปัญหา แล้วกดปิดงาน | ปิดงาน / ผู้บริหาร |
| ลูกค้า/บุคคลภายนอก | `/status/[shareToken]` | ดูวันนัด ทีม/ผู้ติดต่อ สถานะ รูปที่อนุญาต และเวลาอัปเดต | ระหว่างเดินทางเชื่อมข้อมูล ETA จาก tracking session เมื่อมี | Read-only |
| ผู้บริหาร | `/exec` | ดูจำนวนงานในแต่ละสถานะ งานติดขัด คุณภาพ และ SLA | เปิดดูใบงานแบบ read-only | — |
| Admin | `/staff`, `/technicians`, `/orders` | จัดการบัญชี บทบาท ทีม ช่าง/PIN และแก้เหตุฉุกเฉิน | ดู audit trail และสร้าง/ยกเลิกลิงก์ภายนอก | — |

## ขอบเขตข้อมูลในแต่ละหน้า

### `/sales-queue` — ฝ่ายขาย

- แท็บ `จองคิว`, `ต้องแก้ไข`, `รอหัวหน้าตรวจ`, `ยืนยันแล้ว`
- การ์ดงานตีกลับต้องเห็นเหตุผลทันทีโดยไม่ต้องเปิด Drawer
- งานขายตรงมีปุ่ม `แก้ไขและส่งตรวจใหม่`
- งาน BBPS แสดง `แก้ที่ BBPS CRM` และไม่มีฟอร์มแก้ raw payload ใน FloorNow

### `/operations` — หัวหน้าช่าง

- แท็บ `รอตรวจ`, `ส่งกลับแล้ว`, `ส่งคลังแล้ว`
- แสดง missing checklist: ลูกค้า โทร สถานที่ สเปก รูป SKU จำนวน หัวหน้าทีม
- ปุ่ม `ส่งกลับฝ่ายขาย/BBPS` และ `เปิดตรวจและอนุมัติ`
- งาน `returned_sales` ไม่ปะปนกับงานที่รอตัดสินใจ

### `/orders/[jobNo]` — ใบสั่งงานกลาง V3

- ส่วนที่ 1 ข้อมูลต้นทางและ revision
- ส่วนที่ 2 รายละเอียดงาน/รูป/ข้อจำกัดจากฝ่ายขายหรือ BBPS แบบ read-only
- ส่วนที่ 3 รายการหยิบแบบตาราง: SKU, ชื่อ, รุ่น/สี, จำนวนตามแผน, หน่วย, แหล่งของ, จำนวนหยิบจริง
- ส่วนที่ 4 ผู้รับผิดชอบ: ผู้อนุมัติ, หัวหน้าทีม, ช่าง, ผู้รับงานคลัง
- ส่วนที่ 5 การส่งต่อและปุ่มตาม role/status เท่านั้น
- ส่วนที่ 6 ลิงก์ภายนอก: สร้าง, คัดลอก, ปิด, สร้างใหม่
- ส่วนที่ 7 Audit trail

### `/warehouse` — คลัง

- คอลัมน์ `รอรับ`, `กำลังเตรียม`, `ของขาด/ติดปัญหา`, `รอติดตั้ง`
- ห้ามแก้จำนวนตามแผนและ SKU ที่หัวหน้าอนุมัติ
- ต้องระบุจำนวนจริงและแนบรูปก่อนส่งต่อ

### `/work/[personalToken]` — ช่าง

- เห็นเฉพาะงานที่ได้รับมอบหมาย
- ช่างทุกคนกดรับทราบ; เฉพาะหัวหน้าทีมเริ่มงานติดตั้ง
- แสดงรายการของที่คลังหยิบจริง ไม่ให้ช่างเดาจำนวนใหม่
- บังคับรูปตาม checkpoint และลายเซ็นก่อนส่ง CS

### `/cs-tracking` — CS

- ดึงงานจาก `floor_work_orders.status = waiting_cs` ไม่อิง `install_jobs.stage = 6` เพียงอย่างเดียว
- เมื่อบันทึกผลสำเร็จ เปลี่ยนใบสั่งงานเป็น `closed` และอัปเดต `install_jobs.stage = 6`

### `/status/[shareToken]` — ภายนอก

- ลิงก์ถาวรต่อใบสั่งงานและสามารถ revoke/regenerate ได้
- แสดงสถานะ วันเวลา สถานที่โดยย่อ ทีม/ผู้ติดต่อ และรูปที่อนุญาต
- เมื่อช่างเปิด tracking ให้แสดง ETA/อัปเดตล่าสุดโดยไม่เปิด raw GPS
- ซ่อนต้นทุน สต็อก PIN เบอร์ส่วนตัวพนักงาน บันทึกภายใน และ audit trail

## เมนูหลักที่ควรเห็นตามบทบาท

- Sales: `จองคิว`, `ใบสั่งงาน`
- Head technician: `ตรวจและอนุมัติ`, `งานกำลังดำเนินการ`, `ทีมช่าง/PIN`, `ใบสั่งงาน`
- Warehouse: `เตรียมสินค้า`, `คลังวัสดุ`, `ใบสั่งงาน`
- Technician: ไม่มีเมนูหลังบ้าน ใช้หน้าส่วนตัวเท่านั้น
- CS: `รอประเมิน`, `ผลประเมิน`
- Executive: `ภาพรวมผู้บริหาร`
- Admin: เห็นทุกโมดูลหลักและเมนูตั้งค่า

หน้า `/appointments` และ `/pipeline` ให้เป็นเครื่องมือรองสำหรับ Admin/หัวหน้าช่าง ไม่ใช้เป็นจุดเริ่มต้นของ Flow เพื่อไม่ให้สถานะซ้ำหลายชุด

## ช่องว่างที่ต้องแก้จาก V2

1. RPC ส่งกลับต้องเปลี่ยน `floor_work_orders.status` เป็น `returned_sales`
2. ฝ่ายขายต้องมี Inbox งานตีกลับและปุ่มส่งตรวจใหม่
3. BBPS CRM ต้องรับเหตุผลตีกลับและส่ง revision ใหม่กลับ FloorNow
4. ใบสั่งงานต้องดึง `install_jobs.sku/product_skus` และบังคับเลือก SKU สำหรับ BBPS ที่ไม่มี SKU โครงสร้าง
5. มอบหมายช่างต้องทำจากใบสั่งงานได้
6. ต้องสร้าง permanent external share token ต่อใบสั่งงาน
7. CS ต้องอ่าน `waiting_cs` และมี action ปิดงานเป็น `closed`/stage 6
8. เมนูต้องลดหน้าซ้ำและใช้ role-based workspace ตามตารางด้านบน

